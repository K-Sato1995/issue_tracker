query GetProject($id: ID!) {
  project(id: $id) {
    id
    name
    description
    columns {
      title
      issues {
        title
        id
        title
        description
        priority
        deadline
      }
    }
  }
}
